package: libcap2-bin

slices:
  bins:
    essential:
      - libc6_libs
      - libcap2_libs
    contents:
      /usr/sbin/capsh:
      /usr/sbin/getcap:
      /usr/sbin/getpcaps:
      /usr/sbin/setcap:

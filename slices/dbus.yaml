package: dbus

essential:
  - dbus_copyright

slices:
  bins:
    essential:
      - dbus_config
      - dbus-bin_bins
      - dbus-daemon_bins
      - dbus-system-bus-common_config
      - dbus-session-bus-common_config
      - libc6_libs
      - libdbus-1-3_libs
      - libexpat1_libs
      - libsystemd0_libs
    contents:
      /etc/init.d/dbus:
      /usr/lib/dbus-1.0/dbus-daemon-launch-helper:

  config:
    contents:
      /etc/default/dbus:

  extras:
    essential:
      - dbus-system-bus-common_extras
    contents:
      /usr/lib/systemd/system/dbus.service:
      /usr/lib/systemd/system/multi-user.target.wants/dbus.service:

  copyright:
    contents:
      /usr/share/doc/dbus/copyright:

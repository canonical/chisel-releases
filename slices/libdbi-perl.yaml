package: libdbi-perl

essential:
  libdbi-perl_copyright:

slices:

  # If these bins are ever needed, uncomment the following lines
  # and add tests for them.
  # bins:
  #   essential:
  #     libdbi-perl_libs:
  #     perl_bins:
  #   contents:
  #     /usr/bin/dbilogstrip:
  #     /usr/bin/dbiprof:
  #     /usr/bin/dbiproxy:
  #     /usr/bin/dh_perl_dbi:

  libs:
    essential:
      libc6_libs:
    contents:
      /usr/lib/*-linux-*/perl5/5.40/*.pm:
      /usr/lib/*-linux-*/perl5/5.40/**/*.pm:
      /usr/lib/*-linux-*/perl5/5.40/**/*.so:
      /usr/lib/*-linux-*/perl5/5.40/**/*.xst:

  copyright:
    contents:
      /usr/share/doc/libdbi-perl/copyright:

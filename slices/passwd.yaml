package: passwd

essential:
  passwd_copyright:

slices:
  # Binaries used to manage system users. These rely on configuratiopn
  # files provided also by the package.
  bins:
    essential:
      libacl1_libs:
      libattr1_libs:
      libaudit1_libs:
      libbsd0_libs:
      libc6_libs:
      libcrypt1_libs:
      libpam-modules_libs:
      libpam0g_libs:
      libselinux1_libs:
      libsemanage2_libs:
      passwd_config:
      passwd_grpck:
      passwd_grpconv:
      passwd_pwck:
      passwd_pwconv:
      passwd_useradd:
      passwd_userdel:
    contents:
      /usr/bin/chage:
      /usr/bin/chfn:
      /usr/bin/chsh:
      /usr/bin/expiry:
      /usr/bin/gpasswd:
      /usr/bin/passwd:
      /usr/sbin/chgpasswd:
      /usr/sbin/chpasswd:
      /usr/sbin/groupadd:
      /usr/sbin/groupdel:
      /usr/sbin/groupmod:
      /usr/sbin/grpunconv:
      /usr/sbin/newusers:
      /usr/sbin/pwunconv:
      /usr/sbin/usermod:
      /usr/sbin/vigr:  # Symlink to vipw
      /usr/sbin/vipw:

  useradd:
    hint: Create or update user accounts
    essential:
      libacl1_libs:
      libaudit1_libs:
      libc6_libs:
      libselinux1_libs:
      libsemanage2_libs:
    contents:
      /usr/sbin/useradd:

  userdel:
    hint: Delete user accounts
    essential:
      libaudit1_libs:
      libc6_libs:
      libselinux1_libs:
      libsemanage2_libs:
    contents:
      /usr/sbin/userdel:

  pwck:
    hint: Verify integrity of password files
    essential:
      libaudit1_libs:
      libc6_libs:
      libselinux1_libs:
    contents:
      /usr/sbin/pwck:

  pwconv:
    hint: Convert password files to shadow format
    essential:
      libaudit1_libs:
      libc6_libs:
      libselinux1_libs:
    contents:
      /usr/sbin/pwconv:

  grpck:
    essential:
      libaudit1_libs:
      libc6_libs:
      libselinux1_libs:
    contents:
      /usr/sbin/grpck:

  grpconv:
    essential:
      libaudit1_libs:
      libc6_libs:
      libselinux1_libs:
    contents:
      /usr/sbin/grpconv:

  shadowconfig:
    hint: Turn of shadow passwords
    essential:
      base-files_bin:
      dash_bins:
      coreutils_chmod:
      coreutils_chown:
      coreutils_cp-utility:
      coreutils_date:
      coreutils_touch:
      diffutils_cmp:
      passwd_grpck:
      passwd_grpconv:
      passwd_pwck:
      passwd_pwconv:
      sed_bins:
    contents:
      /usr/sbin/shadowconfig:

  scripts:
    essential:
      passwd_shadowconfig:

  # Essential configurations used by useradd(8) and friends.
  config:
    essential:
      base-passwd_data:
    contents:
      /etc/default/useradd:
      /usr/lib/tmpfiles.d/passwd.conf:

  # This slices contains configuration files used by libpam, and these
  # files also include specific configuration files from the libpam libraries.
  # Since libpam functionality is not directly used/supported, this slice may
  # currently not be very useful.
  pam-config:
    contents:
      /etc/pam.d/chfn:
      /etc/pam.d/chpasswd:
      /etc/pam.d/chsh:
      /etc/pam.d/newusers:
      /etc/pam.d/passwd:

  copyright:
    contents:
      /usr/share/doc/passwd/copyright:

package: golang

essential:
  - golang_copyright

slices:
  go-all:
    essential:
      - golang-1.22_all
      - golang-go_bins
      - golang-src_libs

  cgo-support:
    essential:
      # - binutils-${ARCH_TRIPLET}_assembler
      # - binutils-${ARCH_TRIPLET}_linker
      # - cpp-13-${ARCH_TRIPLET}_libs
      # - gcc-13-${ARCH_TRIPLET}_gcc-13
      # - gcc-13-${ARCH_TRIPLET}_libs
      - binutils_assembler
      - binutils_linker
      - gcc_gcc
      - golang_go-all
      - libbinutils_libs
      - libc6-dev_core-libs
      - libc6-dev_headers
      - libgcc-13-dev_core-libs
      - libgcc-13-dev_headers

  copyright:
    contents:
      /usr/share/doc/golang/copyright:

package: libpam0g

slices:
  libs:
    essential:
      - libc6_libs
      - libaudit1_libs
    contents:
      /usr/lib/*-linux-*/libpam.so.0*:
      /usr/lib/*-linux-*/libpam_misc.so.0*:
      /usr/lib/*-linux-*/libpamc.so.0*:

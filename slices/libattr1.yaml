package: libattr1

essential:
  libattr1_copyright:

slices:
  config:
    contents:
      /etc/xattr.conf:

  libs:
    essential:
      libattr1_config:
      libc6_libs:
    contents:
      /usr/lib/*-linux-*/libattr.so.1*:

  copyright:
    contents:
      /usr/share/doc/libattr1/copyright:

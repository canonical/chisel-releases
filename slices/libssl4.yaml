package: libssl4

essential:
  libssl4_copyright:

slices:
  libs:
    essential:
      libc6_libs:
      libzstd1_libs:
      openssl-provider-legacy_libs:
      zlib1g_libs:
    contents:
      /usr/lib/*-linux-*/libcrypto.so.4*:
      /usr/lib/*-linux-*/libssl.so.4*:

  copyright:
    contents:
      /usr/share/doc/libssl4/copyright:

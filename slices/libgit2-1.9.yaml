package: libgit2-1.9

essential:
  - libgit2-1.9_copyright

slices:

  libs:
  # libgssapi-krb5-2:arm64: /usr/lib/aarch64-linux-gnu/libgssapi_krb5.so.2.2
  # libssl3t64:arm64: /usr/lib/aarch64-linux-gnu/libssl.so.3
  # libssl3t64:arm64: /usr/lib/aarch64-linux-gnu/libcrypto.so.3
  # libpcre2-8-0:arm64: /usr/lib/aarch64-linux-gnu/libpcre2-8.so.0.14.0
  # libssh2-1t64:arm64: /usr/lib/aarch64-linux-gnu/libssh2.so.1.0.1
  # zlib1g:arm64: /usr/lib/aarch64-linux-gnu/libz.so.1
  # libc6-dev:arm64: /usr/share/gdb/auto-load/lib/aarch64-linux-gnu/libc.so.6-gdb.py
  # libc6:arm64: /usr/lib/aarch64-linux-gnu/ld-linux-aarch64.so.1
    essential:
      - libgssapi-krb5-2_libs
      - libssl3t64_libs
      - libpcre2-8-0_libs
      - libssh2-1t64_libs
      - zlib1g_libs
      - libc6_libs
      # - libc6-dev_libs

    contents:
      /usr/lib/*-linux-*/libgit2.so.1.9*:

  copyright:
    contents:
      /usr/share/doc/libgit2-1.9/copyright:

package: openssl

slices:
  bins:
    essential:
      - libc6_config
      - libc6_libs
      - libssl3_libs
      - openssl_config
    contents:
      /usr/bin/c_rehash:
      /usr/bin/openssl:

  config:
    contents:
      /etc/ssl/certs/:
      /etc/ssl/openssl.cnf:
      /etc/ssl/private/:
      /usr/lib/ssl/certs:
      /usr/lib/ssl/openssl.cnf:
      /usr/lib/ssl/private:

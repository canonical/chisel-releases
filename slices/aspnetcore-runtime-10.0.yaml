package: aspnetcore-runtime-10.0

essential:
  aspnetcore-runtime-10.0_copyright:

slices:
  # libicu is not needed if the environment is set to invariant by using
  # the environment variable DOTNET_SYSTEM_GLOBALIZATION_INVARIANT=1
  # If globalization is needed, libicu must be included as well.
  # For more information, see:
  # https://learn.microsoft.com/en-us/dotnet/core/runtime-config/globalization
  # https://github.com/dotnet/runtime/blob/main/docs/design/features/globalization-invariant-mode.md
  core:
    essential:
      # NOTE: The notice slices is included here due to the ongoing issue #271 in chisel.
      aspnetcore-runtime-10.0_notice:
      dotnet-runtime-10.0_core:
    contents:
      /usr/lib/dotnet/shared/Microsoft.AspNetCore.App/10.0.*/**:

  standard:
    essential:
      aspnetcore-runtime-10.0_core:
      libicu78_libs:

  notice:
    contents:
      /usr/share/doc/aspnetcore-runtime-10.0/NOTICE.txt.gz:

  copyright:
    contents:
      /usr/share/doc/aspnetcore-runtime-10.0/copyright:

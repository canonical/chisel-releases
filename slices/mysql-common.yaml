package: mysql-common

essential:
  - mysql-common_copyright

slices:
  config:
    contents:
      /etc/mysql/conf.d/*.cnf:
      /etc/mysql/*.cnf.fallback:
      /usr/share/mysql-common/configure-symlinks:

  copyright:
    contents:
      /usr/share/doc/mysql-common/copyright:

package: libldap2

slices:
  libs:
    essential:
      - libc6_libs
      - libgnutls30t64_libs
      - libsasl2-2_libs
    contents:
      /usr/lib/*-linux-*/liblber.so.2*:
      /usr/lib/*-linux-*/libldap.so.2*:

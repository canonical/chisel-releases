package: openssh-sftp-server
# hello world;2i

slices:
  bins:
    essential:
      - libc6_libs
      - openssh-client_bins
    contents:
      /usr/lib/openssh/sftp-server:
      /usr/lib/sftp-server:

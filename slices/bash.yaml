package: bash

slices:
  config:
    contents:
      /etc/bash.bashrc:
      /etc/skel/.bash_logout:
      /etc/skel/.bashrc:
      /etc/skel/.profile:

  bins:
    essential:
      - bash_config
      - libc6_libs
      - libtinfo6_libs
    contents:
      /bin/bash:
      /bin/rbash:
      /usr/bin/clear_console:

package: libgprofng0

slices:
  libs:
    essential:
      - libbinutils_libs
      - libc6_libs
      - libgcc-s1_libs
      - libstdc++6_libs
      - zlib1g_libs
    contents:
      /usr/lib/*-linux-*/gprofng/libgp-collector.so:
      /usr/lib/*-linux-*/gprofng/libgp-collectorAPI.so:
      /usr/lib/*-linux-*/gprofng/libgp-heap.so:
      /usr/lib/*-linux-*/gprofng/libgp-iotrace.so:
      /usr/lib/*-linux-*/gprofng/libgp-sync.so:
      /usr/lib/*-linux-*/libgprofng.so.0*:

package: libnspr4

slices:
  libs:
    essential:
      - libc6_libs
    contents:
      /usr/lib/*-linux-gnu/libnspr4.so:
      /usr/lib/*-linux-gnu/libplc4.so:
      /usr/lib/*-linux-gnu/libplds4.so:

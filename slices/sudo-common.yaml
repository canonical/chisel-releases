package: sudo-common

essential:
  sudo-common_copyright:

slices:
  config:
    contents:
      /etc/pam.d/sudo:
      /etc/pam.d/sudo-i:
      /etc/sudoers:
      /etc/sudoers.d/:

  copyright:
    contents:
      /usr/share/doc/sudo-common/copyright:

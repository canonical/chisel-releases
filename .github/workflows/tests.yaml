name: Tests

on:
  push:
    branches:
      - "ubuntu-*"
  pull_request:
    branches:
      - "ubuntu-*"

jobs:
  installability:
    name: Installability
    uses: canonical/chisel-releases/.github/workflows/install-slices.yaml@main

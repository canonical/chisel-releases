summary: Integration tests for xdg-user-dirs

execute: |
  rootfs="$(install-slices xdg-user-dirs)"

  test -f ${rootfs}/etc/xdg/autostart
  test -f ${rootfs}/etc/xdg/user-dirs.conf
  test -f ${rootfs}/etc/xdg/user-dirs.defaults

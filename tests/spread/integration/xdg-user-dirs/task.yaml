summary: Integration tests for xdg-user-dirs

execute: |
  rootfs="$(install-slices xdg-user-dirs_scripts)"

  test -d ${rootfs}/etc/xdg/autostart
  test -d ${rootfs}/etc/xdg/user-dirs.conf
  test -d ${rootfs}/etc/xdg/user-dirs.defaults

summary: Integration tests for sensible-utils

variants:
    - select-editor
    - sensible-browser
    - sensible-editor
    - sensible-pager
    - sensible-terminal

execute: bash -ex ./test_${SPREAD_VARIANT}.sh

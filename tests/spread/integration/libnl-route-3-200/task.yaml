summary: Integration tests for libnl-route-3-200

execute: |
  rootfs="$(install-slices libnl-route-3-200_libs)"
  arch=$(uname -m)-linux-gnu

  test -f ${rootfs}/usr/lib/${arch}/libnl-route-3.so.200

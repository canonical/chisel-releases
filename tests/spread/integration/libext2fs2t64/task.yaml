summary: Integration tests for libext2fs2t64

execute: |
  rootfs="$(install-slices libext2fs2t64_libs)"

  # Library should be installed somewhere under /usr/lib
  chroot "${rootfs}" sh -c 'find /usr/lib -name "libext2fs*.so*" -print -quit | grep -q .'

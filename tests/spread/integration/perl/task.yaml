summary: Integration tests for perl

execute: |
  rootfs=$(install-slices base-files_base bash_bins coreutils_bins perl_bins)
  mkdir -p "$rootfs/dev"
  touch "$rootfs/dev/null"
  chroot "$rootfs" perl -e 'print "hello world\n";' | grep "hello world"

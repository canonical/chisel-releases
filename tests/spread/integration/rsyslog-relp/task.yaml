summary: Integration tests for rsyslog-relp

execute: |
  rootfs="$(install-slices rsyslog-relp_modules)"

  for f in "${rootfs}"/usr/lib/*/rsyslog/imrelp.so "${rootfs}"/usr/lib/*/rsyslog/omrelp.so; do
    test -f "$f"
    head -c4 "$f" | grep -Fiq "ELF"
  done
